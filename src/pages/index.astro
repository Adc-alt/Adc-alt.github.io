---
import Base from "../layouts/Base.astro";
import Boot from "../components/Boot.astro";
import HomeContent from "../components/HomeContent.astro";
---

<Base>
  {/* La pantalla de arranque solo existe aquí, en la raíz. Los enlaces
      profundos (/proyectos/x) y /work/ no la ven nunca.
      Va al slot "overlay" para salir de <main> y poder taparlo todo. */}
  <Boot slot="overlay" />
  <HomeContent />
</Base>
